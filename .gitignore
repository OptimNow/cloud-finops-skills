# Internal
INTERNAL_NOTES.md

# Environment
.env
.env.*

# OS files
.DS_Store
Thumbs.db
NUL

# Claude Code local settings (any depth - root, skills/cloud-finops/, etc.)
.claude/settings.local.json
**/.claude/settings.local.json

# Claude Code local hooks config
claude-hooks-settings.json

# Claude Code worktrees (embedded git repos)
.claude/worktrees/

# Local-only nested .claude/ directories (e.g. skills/cloud-finops/.claude/)
skills/cloud-finops/.claude/

# Local working notes / plan drafts
plan-*.md
plan-remediation-*.md
note-*.md

# Pipeline (private until public release)
pipeline/

# Reference file backups
skills/cloud-finops/references/.backups/

# Distribution archive
cloud-finops.zip

# Installer build output (./install.sh --tool chatgpt|gemini|claude-projects
# writes here; a chatgpt build alone is ~57 untracked files)
dist/

# Python build artefacts (mcp_server/)
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
mcp_server/build/
mcp_server/dist/
mcp_server/.venv/
# alpic.json's installCommand creates a venv at the repo root, and so does any
# local rehearsal of it
.venv/

# LinkedIn post drafts
linkedin-post-*.md

# Review adjudication
review-adjudication.md
